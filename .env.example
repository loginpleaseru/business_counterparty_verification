# Create .env from this file and provide the key yourself.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=qwen/qwen3-30b-a3b-instruct-2507

MCP_URL=http://localhost:8001/mcp
MCP_TIMEOUT_SECONDS=30
ANALYSIS_TIMEOUT_SECONDS=90
SESSION_TTL_SECONDS=3600

# mongo (default), mock or postgres
REPOSITORY_BACKEND=mongo
MOCK_DATA_PATH=data/counterparties.json
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/counterparties

# MongoDB container and seed
MONGO_IMAGE_TAG=7.0
MONGO_ROOT_USERNAME=contractors_admin
MONGO_ROOT_PASSWORD=change_me
MONGO_DATABASE=counterparties
MONGO_COLLECTION=reports
MONGO_READ_COLLECTION=counterparty_cards
MONGO_PORT=27017

# Used only when the API is started directly on the host.
# Docker Compose overrides this URL and uses the hostname "mongo".
MONGODB_URL=mongodb://contractors_admin:change_me@localhost:27017/counterparties?authSource=admin
MONGODB_DATABASE=counterparties
MONGODB_COLLECTION=counterparty_cards
